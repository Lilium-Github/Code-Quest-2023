import sys

SEPARATOR = ","

cases = int(sys.stdin.readline().rstrip())

for caseNum in range(cases):
    line = sys.stdin.readline().rstrip()
    scorecard = line.split(SEPARATOR)

    points = 0

    for index, frame in enumerate(scorecard):

                




    print(points)